(bug batch, R053 physics P0 + bare mode — 2026-06-06 driving the merged physics car) LEAKY BARE-MODE COMPOSITION + ROUGH PHYSICS SPIKE defaulted ON. Bugs observed: (1) HEADLIGHTS float on the ground, not glued to the car — updateHeadlights samples terrainHeightAt(x,z) instead of the car's rigid-body pose (position+quaternion), so on pitch/roll/air the lamps stay on the terrain. (2) CAR HOVERS above the terrain (ride-height / suspension rest wrong; chassis sits a constant gap above ground). (3) CAR CATCHES AIR a lot (raycast-vehicle suspension too bouncy). (4) STEERING does nothing (vehicle.rs has steer logic + intents.steer is passed; car doesn't turn — investigate input wiring / lateral grip / steering authority). (5) PHANTOM STREETLIGHT HALOS in bare mode — updateStreetlightPool still places the ~6 pooled PointLights at streetlight positions even though bare hides the streetlight MESHES -> floating glows with no lamp. (6) 'the entire city is still in chunkgen' — bare gates mesh ADDS but the world-gen systems still run; clarify intended bare behavior. ROOT:  gates mesh adds but NOT the dependent per-frame systems (streetlight pool, etc.), and the physics car is an unhardened P0 SPIKE (hover/steer/air/headlights) that was defaulted ON as if finished. FIX: make bare gate the SYSTEMS not just meshes; glue headlights to the car pose; harden the vehicle (ride height, steering, suspension) or default physics OFF until tuned. Project: HOD.
