(decision+task) HOD RUST CRATE/FOLDER LAYOUT — conclusions from the 2026-06-06 discussion (do NOT lose again). (1) FOLDER NAMING: drop the redundant 'hod-' prefix from crate FOLDERS -> bare rust/chunkgen, rust/sim, rust/game, rust/physics; KEEP 'hod-' on the crate [package] name (hod-chunkgen, hod-sim, ...). This mirrors rapid-game's verified convention: bare folder (components, physics, core) -> prefixed crate name (rg-components, rg-physics, rg-core). RATIONALE: folders never collide (just paths); crate NAMES do — a bare 'core' clashes with std's core crate (rapid-game renames core->rg-core for exactly this), and generic names (sim/game/gen) risk colliding with transitive crates.io deps in the merged build graph. So the prefix belongs on the crate name ONLY. (2) CRATE CONSOLIDATION: the split is over-split for ~6k lines — hod-chunkgen and hod-sim have IDENTICAL deps (just wasm-bindgen+console_error_panic_hook, both cdylib) so splitting them buys ONLY two separate wasm artifacts, nothing dependency-wise; they could be ONE 'hod' wasm crate. hod-game (bevy_ecs/bevy_app/rg-components) and hod-physics (Rapier) are the ones with a real dependency-isolation reason to stay separate. Target: collapse the light wasm crates; keep engine/physics consumers separate by dep weight; bare folders + prefixed names throughout. (3) hod-physics was JUST added (another hod- folder) — fold it into this cleanup. (4) EXECUTION: rename folders + workspace members/path refs; crate NAMES unchanged so wasm artifacts (hod_chunkgen.js) + -p flags don't change -> low-risk, mechanical. Do when rust/ is stable (physics merged). Project: HOD.
