(tech-debt, decided-long-ago NEVER EXECUTED) FROZEN-TS LEGACY SEPARATION: the pre-Rust TS game logic (src/world, src/systems, src/vehicles, the TS sim/TsSim) was directed LONG AGO to be pulled OUT of the live src/ tree into a clearly-marked LEGACY/FROZEN area (intent on record HOD-D003/D010: TS sim retired/legacy, kept as a frozen example). NEVER DONE: no legacy/ folder exists; legacy TS stays interleaved with the live viewer in src/ -> continuing confusion about what's live vs dead. The folder-move execution was never captured as a tracked TICKET (cache fts empty) so nothing enforced it. FIX: (1) track it now; (2) execute the reorg into a recognizable layout (a frozen legacy/ area, live viewer cleanly separate) AFTER the physics agent merges (it touches src/render); coordinate so we don't collide. Project: HOD.
