(feature) timestamped agent replies + per-session timestamped transcript dump — Chris runs multiple terminals and cannot reconstruct what was said when/where; replies should lead with a [YYYY-MM-DD HH:MM] stamp (clock via hook-injected time or a date call), and a kit query should dump a session's conversation with the timestamps that already exist in ~/.claude/projects/*.jsonl transcripts (2026-07-09)
