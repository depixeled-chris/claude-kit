feedback AGENT-ORCHESTRATION PROCESS FAILURE (HOD, 2026-06-05): panic-killed a running background agent over a TRANSIENT mid-edit HMR error; killing a local agent REVERTED its uncommitted work -> lost a near-complete facade rebuild AND wasted its tokens (and would have re-burned them redoing it). TWO RULES: (1) do NOT reactively kill a running agent over a transient/in-progress error (the agent was at 'build green'); diagnose first, the live build self-heals when the agent finishes. (2) Any agent that EDITS CODE must run in an ISOLATED git worktree (Agent isolation:'worktree') so it (a) never breaks the live working tree the maintainer is actively playing, and (b) a stop/kill can't silently revert/lose its work — it commits on its own branch and the orchestrator merges only after it builds + meets the bar. Never run a code-editing agent against the maintainer's live dev tree.
