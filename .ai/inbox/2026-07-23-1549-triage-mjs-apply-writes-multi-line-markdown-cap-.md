(bug) triage.mjs --apply writes multi-line markdown cap text into ticket frontmatter title unsanitized — a cap whose text starts '# Heading' + bullet body produced 'title: # ...' (YAML comment = BLANK title) and dumped the whole body INSIDE the frontmatter block (HOD-T422..T425, 2026-07-23 triage of 40 caps). Title extraction must take the first line, strip leading '#', quote it, and cap length; body stays in ## Description only. Hand-repaired the 4 HOD tickets same day.
