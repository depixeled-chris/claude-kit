(decision) Dispatch-ladder enforcement shape (KIT-T151, Chris 2026-07-23): (1) kit work agents (researcher/code-reviewer/refactorer/test-author) pin model: opus in frontmatter — a delegation never inherits the session model; (2) new dispatch-guard.mjs PreToolUse(Task|Agent) hook blocks the SILENT fable inherit (no model on call + no frontmatter pin + fable session transcript); (3) explicit model:'fable' stays legal — it is a chosen tier and dispatch.tiers deep expands to it; escape for a deliberate model-less inherit = inline [allow-fable: <reason>]. Rationale: fable is expensive; ladder compliance was memory-dependent (lived failure: 5 researcher agents inherited fable in groovegrid). Recorded at the kit source per the dated-model-judgment rule.
