(decision+task) ONE RUST CORE -> ONE WASM. The game is in Rust = ONE core, not three fragmented wasm modules. Today dev builds THREE wasms (hod-sim, hod-chunkgen, hod-physics — three separate cdylib crates each loaded separately by TS). Consolidate to ONE 'hod' core crate -> ONE hod.wasm: world-gen + sim + physics as modules inside it, unify the wasm-bindgen exports, collapse the three build:wasm* npm scripts to one, repoint the TS loaders (chunkgenWasm/WasmSim/PhysicsSim) to instantiate ONE module. The 'keep physics separate for dep-isolation' argument is void — it's all one game core, Rapier in the same wasm is correct. Extends/supersedes the crate-layout cap (bare folders + prefixed name) to ALSO mean one core crate, not 3. The physics agent bolted on a 3rd wasm instead of folding in — don't let agents fragment the core. Project: HOD.
