#!/bin/sh
# post-rewrite — hydrate the SQLite cache after pull --rebase / commit --amend (KIT-T097)
DIR=$(CDPATH= cd "$(dirname "$0")" && pwd)
node "$DIR/../scripts/hydrate-db.mjs" >/dev/null 2>&1 || true
